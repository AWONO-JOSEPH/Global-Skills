Quand tu as importé tes données SQLite vers PostgreSQL, les IDs ont été insérés manuellement (1, 2, 3...) mais la séquence PostgreSQL (qui génère les nouveaux IDs automatiquement) est restée à 1. Du coup quand Laravel essaie d'insérer un nouvel enregistrement, PostgreSQL génère l'ID 2 qui existe déjà.

La solution — Réinitialiser les séquences
Va dans ton Postgres Render → PSQL Console et exécute ces commandes pour chaque table :