Utilise cron-job.org gratuitement pour pinger ton backend toutes les 10 minutes :

Crée un compte sur cron-job.org
Crée un nouveau cron job :

ChampValeurURLhttps://global-skills.onrender.com/api/pingIntervalToutes les 10 minutes

Crée la route ping dans Laravel Backend/routes/api.php :

phpRoute::get('/ping', function () {
    return response()->json(['status' => 'ok']);
});

Solution 2 — Ajouter les headers CORS dans Nginx
Même quand Laravel ne répond pas (504), Nginx peut quand même retourner les headers CORS. Modifie Backend/nginx.conf :
nginxserver {
    listen 10000;
    server_name _;
    root /var/www/public;
    index index.php index.html;

    # Headers CORS globaux
    add_header 'Access-Control-Allow-Origin' 'https://global-skills.vercel.app' always;
    add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, DELETE, OPTIONS' always;
    add_header 'Access-Control-Allow-Headers' 'Authorization, Content-Type, Accept, X-Requested-With' always;
    add_header 'Access-Control-Allow-Credentials' 'true' always;

    location / {
        # Gérer les requêtes OPTIONS (preflight)
        if ($request_method = 'OPTIONS') {
            add_header 'Access-Control-Allow-Origin' 'https://global-skills.vercel.app' always;
            add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, DELETE, OPTIONS' always;
            add_header 'Access-Control-Allow-Headers' 'Authorization, Content-Type, Accept, X-Requested-With' always;
            add_header 'Access-Control-Allow-Credentials' 'true' always;
            add_header 'Content-Length' '0';
            add_header 'Content-Type' 'text/plain';
            return 204;
        }

        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        fastcgi_pass 127.0.0.1:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
    }

    location ~ /\.(?!well-known).* {
        deny all;
    }
}